[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "pingpong-predictor-advanced"
version = "0.2.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
